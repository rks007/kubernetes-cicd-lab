apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-app-deployment
  namespace: kustom
  labels:
    app: blog-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: blog-app
  template:
    metadata:
      labels:
        app: blog-app
    spec:
      containers:
        - name: blog-app-container
          image: rks007/kustomize:668255ddee4db4f9f2ae913d89ad9c1ee7ff0ecc
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: kustom-config