apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-app-deployment
spec:
  template:
    spec:
      containers:
        - name: blog-app-container
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"